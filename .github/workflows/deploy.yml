name: "Deploy Auth Service"

on:
  push:
    branches:
      - "**"

jobs:
  deploy:
    permissions:
      id-token: write
      contents: read
      packages: write
    uses: unfettered-one/Infra/.github/workflows/main.yml@f5b0e2838d707a001dd60bf6e0d5c1c46eb94fb7
    with:
      role_arn: "arn:aws:iam::815817733987:role/terraform-deploy-role-dev"
      aws_region: "ap-south-1"
      working_directory: "infra"
      lambda_code_path: "auth_service"
      build_image: true
      lambda_handler: "app.handler"
    secrets: inherit
