{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Service",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "auth_service.main:app",
                "--reload",
                "--host",
                "0.0.0.0"
            ],
            "justMyCode": false,
            "envFile": "${workspaceFolder}/.local/.env",
            "env": {
                "PYTEST_ADDOPTS": "--no-cov",
            }
        }
    ]
}